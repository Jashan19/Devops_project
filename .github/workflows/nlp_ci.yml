name: NLP CI

on:
  push:
    branches:
      - main

jobs:
  nlp-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Dummy step
        run: echo "NLP CI placeholder – no job yet"
